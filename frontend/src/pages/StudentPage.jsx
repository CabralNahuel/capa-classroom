import React from 'react';
import { Container, Typography } from '@mui/material';

const StudentPage = () => {
  return (
    <Container>
      <Typography variant="h4">Student Details Page</Typography>
      <Typography>This page will show detailed reports for a specific student.</Typography>
    </Container>
  );
};

export default StudentPage;
